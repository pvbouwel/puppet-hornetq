#!/bin/sh
<% if @owner != 'root' %>
# Root should not run this script
if [ "$UID" = "0" ]
then
  echo "Dear root, please su to an appropriate user (<%= @owner %>)."
  exit 666
fi
<% end %>
touch <%= @_confdir %>/STOP_ME;